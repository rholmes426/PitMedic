name: Sign PitMedic 0.6.0.12

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/sign-v0.6.0.12.yml"

permissions:
  contents: write
  id-token: write

jobs:
  sign:
    uses: ./.github/workflows/sign-release.yml
    with:
      release_tag: v0.6.0.12
