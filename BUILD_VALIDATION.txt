PitMedic v0.4.4.0 validation
============================

Validated in the packaging environment:
- The package is based on the saved v0.4.2.1 full development package.
- Project, assembly, file, and informational versions are set to 0.4.4.0.
- All prior v0.4.2.1 application source, XAML, repair knowledge, assets, and documentation remain present.
- The complete GPL-3.0 license is included and the project declares GPL-3.0-or-later.
- Public contribution, security, privacy, and code-signing policies are present.
- No certificate, private key, API token, or signing identifier is stored in the package.
- GitHub CI builds the Windows target from the checked-in source.
- The release workflow checks out an explicit tag, confirms the tagged commit, and submits the workflow-produced artifact to SignPath.
- Release signing requires the protected production-signing environment and manual SignPath approval.
- Signature validation requires a valid SignPath Foundation publisher certificate and timestamp before creating the SHA-256 release manifest.
- Unsigned development artifacts remain visibly separate from official signed releases.
- Source inspection found no remaining v0.4.2.1 runtime version strings outside historical release notes.

Windows CI result:
GitHub Actions build-validation run #1 completed successfully on a Windows runner for commit 06f3f9439d179bf9a99bb2ebd3dfd09f22d092b2. Dependency restore, .NET 10 Release compilation, the self-contained win-x64 release build, version checks, unsigned-build manifest creation, and validation-artifact upload all passed. The uploaded artifact reported SHA-256 digest f729e79392d3012f2f3c79054d5bc93287fbd5b354d5b97af179bd6abcb0bab1.

Authenticode limitation:
The unsigned validation artifact is not an official release. Publisher, signature, and timestamp verification will remain intentionally blocked until SignPath Foundation approves the project and returns the first signed artifact.
